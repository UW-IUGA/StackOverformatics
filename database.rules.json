{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "queue": {
      // "$queuer": {
      //   ".read": "$queuer.uid === auth.uid",
      //   ".write": "$queuer.uid === auth.uid"
      // }
    }
  }
}